namespace PD.Workademy.Exercises.Tasks
{
    public static class SortedStringByCustomRule
    {
        public static string Test(string stringOne, string stringTwo)
        {
            /*
                Write a method that receives two strings and returned second one sorted by rule defined by the first string.
                First string has unique (non-repeated) letters. Both strings consist only of alphabet characters.
                If any character from the second string is not in first string it can be on any position in result (ordered) string.*/
            string output = "";
            return output;
        }
    }
}
